[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "scalper"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["constants*", "core*", "regression*", "classification*"]
